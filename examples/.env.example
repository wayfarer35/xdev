### General project
PROJECT_DIR=./src

### PHP image
# Use a pre-built php image (no local Dockerfile build)
# Format: name:tag, e.g. xdev-php:8.4-fpm-bookworm
PHP_TAG=8.4-fpm-bookworm
PHP_IMAGE=xdev-php:${PHP_TAG}

### PHP extensions control
# Runtime: you can control per-version extension toggles using variables of the
# form `PHP_<VER_NO_DOT>_<EXTNAME>=1` (enable) or =0 (disable). Examples:
#   PHP_84_XDEBUG=1    -> enable xdebug in php 8.4 service
#   PHP_82_PDO_MYSQL=1 -> enable pdo_mysql in php 8.2 service
# Note: services expose `PHP_VER_NODOT` (e.g. 84) so entrypoint scans for matching vars.
ALLOW_EXTENSIONS_FILE=./config/php/extensions.allowed
# Global runtime ENABLE_EXTENSIONS fallback (if set) — comma separated
ENABLE_EXTENSIONS=

# Per-version extension toggles are provided via service-specific env files in
# `./config/env/` (e.g. `config/env/php-8.4.env`). Keep global settings here.

### Ports
NGINX_PORT=8080
PHP_FPM_PORT=9000
MYSQL_PORT=3306
REDIS_PORT=6379

### MySQL credentials (change for production)
MYSQL_ROOT_PASSWORD=rootpass
MYSQL_DATABASE=app_db
MYSQL_USER=app
MYSQL_PASSWORD=apppass

### Paths (host-side binds)
# project www root
PROJECT_DIR=./src
# config, logs and data directories are mounted under these paths
CONFIG_DIR=./config
LOGS_DIR=./logs
DATA_DIR=./data

### Notes
# Per-extension allow/deny list is in the file pointed by ALLOW_EXTENSIONS_FILE.
# The entrypoint still uses ENABLE_EXTENSIONS to decide which of the preinstalled
# extensions are symlinked into PHP conf.d at container startup.
